<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aoneill98.organiser2">

    <!-- Required permissions should be here above application tag -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="StartUp2"
        android:roundIcon="@drawable/startup3"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".Register" />
        <activity android:name=".MainMenu" />
        <activity
            android:name=".Statements"
            android:screenOrientation="landscape" />
        <activity
            android:name=".AddStatement"
            android:screenOrientation="landscape" />
        <activity android:name=".Products" />
        <activity
            android:name=".EditStatement"
            android:screenOrientation="landscape" />
        <activity android:name=".ContactUs" />
        <activity android:name=".Contacts" />
        <activity android:name=".MapsActivity"></activity>
    </application>

</manifest>